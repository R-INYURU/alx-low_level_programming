#include <stdio.h>

/**
 * main - check the code
 *
 * Return: always 0
 */
int main(void)
{
	printf("(A tortoise, having pretty good sense of a hare's nature, challenges on to a race.)\n");
	return (0);
}
